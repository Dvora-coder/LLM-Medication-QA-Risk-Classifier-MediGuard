The full corpus file ("knowledge_corpus") contains every individual passage that was indexed for retrieval.
The "overlap" file, by contrast, lists only those passages that appear (or are very similar) in more than one source document—
the overlap file is used to identify if there is any overlap among some of the passages.
